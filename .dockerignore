.git
.env
.env.*
!.env.example

# Dependencies — reinstalled inside the image
node_modules
**/node_modules

# Build artifacts — rebuilt inside the image
dist
**/dist

# Note: apps/web is intentionally NOT ignored — the web image
# (apps/web/Dockerfile) builds from this same root context. The API
# Dockerfile simply doesn't COPY apps/web, so the API image is unaffected.

# Test/dev tooling
packages/testing/src
coverage
*.log
